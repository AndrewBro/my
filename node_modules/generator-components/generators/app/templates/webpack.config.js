var pkg = require('./package.json')

var config = {
    entry: {

    },
    output: {
        filename: '[name].js',
        libraryTarget: 'umd'
    },
    module: {
        loaders: [
            {
                test: /\.(js)$/,
                loader: 'babel',
                query: {
                    presets: ['es2015']
                }
            }
        ]
    }
}

config.entry[pkg.name] = './src/index.js'

module.exports = config
